# lcondliffe Homebrew tap

Homebrew formulae for Luke Condliffe's command-line tools.

## Install gitling

```sh
brew install lcondliffe/tap/gitling
```

`gitling` release formulae are generated from the cross-compiled GitHub Release archives in [`lcondliffe/gitling`](https://github.com/lcondliffe/gitling).
